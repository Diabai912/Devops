name: Testing github workflow
on: push
jobs:
  integration-continue:
    name: Démonstration CI
    runs-on: ubuntu-latest

    env:
      STEP1: "Le code vient d'être poussé sur github"


    steps:
      -name: Etape1 - Initialisation 
      run: 
          echo " Démarrage du pipeline d'intégration continue"
          echo "Step1= $STEP1"
      -name: Etape2 - Recuperation du code
      run:
       echo "Récupération du code source "
       echo "STEP2= ${{vars.STEP2}}"
      # - run: echo " etape 3 Build"
      # - run: echo " etape 4 Tests"
      # - run: echo " etape 5 Packaging"
      # - run: echo " etape 6 fin du pipeline"
        